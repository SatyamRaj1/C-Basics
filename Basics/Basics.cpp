#include<iostream>
using namespace std;

int main()
{
/*  void basic(void);
    void ifelse(void);
    string a1;
    getline(cin,a1);  //cin takes first word only(before space) so getline for complete line
    cout<<a1.length();
    int x,y,z;
    
    cin>>x;
    char t='h';  //single quotes for characters
    string p="satyam";  //string double quotes
    cout<<t<<p;
    double
    line
    comments
    for(int i,k=0;i<20,i<50;i++,k+=5)
    {
        cout<<x;
        
    }
    const int m=12;   //m can't be change afterwards
    
    
    if (x>12)
    { cout<<"satyam";}
    else
    {cout<<"player";}
    
    //another syntax for ifelse
    string result=(x>12)? "trpdf":"wefw";
    cout<<result; */
    /*string pro;  //counting number of alphabets
    getline(cin,pro);
    int count=0;
    for(int i=0;i<pro.length();i++)
    {if (pro[i]=='a')
    count++;
    }
    cout<<count;*/
/*
    //sum of digits
    //loops
    int z,y,x,c=0;
    cin>>x;
    y=x;
    z=x;
    //for loop
    for(x;x>0;x=x/10)
    { c+=x%10;
    }
    cout<<c<<'\n';   //endl can also be used
    //while loop
    c=0;
    while(y>0)
    { c+=y%10;
    y=y/10;
    }
    cout<<c<<endl;
    //do while loop
    c=0;
    do
    {c+=z%10;
    z/=10;
    }while(z>0);
    cout<<c;
*/
/*
    //Arrays
    int x[]={1,2,34,32,5},y[5]={1,2,23,34};//if last element missing it is 0
    cout<<"hello";
    for(int i=0;i<5;i++)
    {cout<<x[i]<<endl;
    cout<<y[i]<<endl;}
    string a[]={"raj","vj","batra"};
    int max=0;
    for(int i=0;i<sizeof(a)/sizeof(a[0]);i++)  //sizeof(a) will give memory size of array and sizeof(a[0)) for any element of array  so it will give number of elements
    {if (a[i].length()>max)
    max=a[i].length();
    }
    cout<<max;
    
    
    int a[]={1,2,5,2,6,32,8,34,90,34,6,4},l;
        l=sizeof(a)/sizeof(a[0]);
        cout<<l;
*/
    return 0;
}



